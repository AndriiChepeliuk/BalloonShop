<UserControl x:Class="BalloonShop.UserControls.CategoryElement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Name="categoryElement">

    <Button Padding="20" Width="230" Margin="15 0" FocusVisualStyle="{x:Null}" Click="Button_Click" Style="{StaticResource categoryButton}">
        <StackPanel VerticalAlignment="Center">
            <Image Source="{Binding ElementName=categoryElement, Path=ImageSource}" Width="200" Height="200" 
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <TextBlock Text="{Binding ElementName=categoryElement,Path=Title}" FontSize="18" FontWeight="SemiBold" 
                       TextAlignment="Center" Margin="0 30 0 0"/>
        </StackPanel>
    </Button>

</UserControl>
